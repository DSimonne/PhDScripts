import GasTool as gt
import time
mv(heater3, 0)
gt.condE()
# 50ml/min 0.3Bar
# flow(Arg=32, COg=0, O2g=8,NOg=0, H2g=7.81, mixg=6, press=0.3 )
time.sleep(300)

gt.tloopFastOff(0,1.5,150)

time.sleep(180)
gt.stflow()